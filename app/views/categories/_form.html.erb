<%= form_for(@category) do |f| %>
    <% if @category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>
          <ul>
            <% @category.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">hinzufügen</h3>
      </div>
      <div class="panel-body">

        <div class="col-xs-6">
          <%= f.check_box :income %>
          <%= f.label :income %>
          <%= f.hidden_field :user_id, :value => current_user.id %>
        </div>

        <div class="col-xs-6">
          <div class="form-group">
            <%= f.check_box :active, :checked => true %>
            <%= f.label :active %>
          </div>
        </div>

        <div class="col-xs-6">
          <div class="form-group">
            <label class="" for="">Name:</label>
            <%= f.text_field :name, :class =>"form-control", :placeholder => "Kategorie Name" %>
          </div>
        </div>
        <div class="col-xs-6">
          <div class="form-group">
            <label class="" for="">Name:</label>
            <%= f.text_field :notice, :class =>"form-control", :placeholder => "Kategorie Notiz" %>
          </div>
        </div>
      </div>
      <div class="panel-footer"><%= f.submit "Speichern" , :class => "btn btn-default" %><div>
      </div>
    </div>
<% end %>
